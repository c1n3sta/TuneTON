apiVersion: apps/v1
kind: Deployment
metadata:
  name: audio-processing-service
  namespace: tuneton
spec:
  replicas: 2
  selector:
    matchLabels:
      app: audio-processing-service
  template:
    metadata:
      labels:
        app: audio-processing-service
    spec:
      containers:
        - name: audio-processing-service
          image: tuneton/audio-processing-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: "production"
            - name: RABBITMQ_URL
              value: "amqp://rabbitmq:5672"
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: auth-secrets
                  key: jwt-secret
            - name: STORAGE_PATH
              value: "/data/processed"
          volumeMounts:
            - name: audio-storage
              mountPath: "/data"
          resources:
            requests:
              cpu: "200m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: audio-storage
          persistentVolumeClaim:
            claimName: audio-storage-pvc
