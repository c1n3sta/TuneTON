apiVersion: v1
kind: Secret
metadata:
  name: auth-secrets
  namespace: tuneton
type: Opaque
data:
  # Replace with your actual Telegram bot token (base64 encoded)
  telegram-bot-token: ""
  # Generate a secure JWT secret (e.g., using: openssl rand -base64 32)
  jwt-secret: ""
