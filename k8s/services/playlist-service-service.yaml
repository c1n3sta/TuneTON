apiVersion: v1
kind: Service
metadata:
  name: playlist-service
  namespace: tuneton
  labels:
    app: playlist-service
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: playlist-service
