apiVersion: v1
kind: Service
metadata:
  name: streaming-service
  namespace: tuneton
  labels:
    app: streaming-service
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: streaming-service
