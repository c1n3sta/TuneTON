apiVersion: v1
kind: Service
metadata:
  name: track-service
  namespace: tuneton
  labels:
    app: track-service
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: track-service
