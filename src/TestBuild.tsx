import React from 'react';

const TestBuild: React.FC = () => {
  return (
    <div>
      <h1>Test Build</h1>
      <p>This is a simple test component to check if the build works.</p>
    </div>
  );
};

export default TestBuild;